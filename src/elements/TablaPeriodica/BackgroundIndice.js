import styled from 'styled-components'

const BackgroundIndice = styled.div`
    background: ${props => props.background};
    width: 30px;
    height: 30px;
    border-radius: 10px;
`

export default BackgroundIndice
